/target
.idea/
.fleet/
# Ignore downloaded map tiles (centralized cache symlinked into assets/tiles)
/assets/tiles
/assets/*.tile.png
/tmp/
# macOS bundle transient files
macos/tmp/
macos/assets/tiles
/.worktrees/
/.superpowers/
config.toml
# Real feeder secrets for the k3s Pi deployment (see infra/k3s-pi/README.md)
infra/k3s-pi/secret.yaml
# Large raw I/Q capture fixtures, too big for git (see fixtures README for provenance)
crates/adsb-client/tests/fixtures/*.bin
